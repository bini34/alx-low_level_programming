#include "main.h"
/**
 * _isupper - a function that checks for uppercase characte
 * @c: a charcter that check
 * Return: return 1 if c is uppercase or 0 other
 */
int _isupper(int c)
{
	if (isupper(c))
		return (1);
	else
		return (0);
}
