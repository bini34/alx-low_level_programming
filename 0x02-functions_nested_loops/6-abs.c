#include "main.h"
/**
 * _abs - function that computes the absolute value of an integer
 *
 * @n: a number that use
 *
 * Return: the absolute value of an integer
 */
int _abs(int n)
{
	return (abs(n));
}
