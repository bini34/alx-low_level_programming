#include "main.h"
/**
 * add - a function that adds two integers and returns the result
 *
 * @x: first intiger
 * @y: second intiger
 *
 * Return: the sum of two int
 */
int add(int x, int y)
{
	return (x + y);
}
